[project]
name = "ucloudshell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "docker>=7.1.0",
    "flask>=3.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.5",
]
